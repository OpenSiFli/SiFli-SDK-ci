sync_to_github:
  stage: sync_to_github
  tags: [docker]
  before_script:
    - apt-get update && apt-get install -y git
  script:
    - echo "[sync_to_github] 推送GitLab主分支到GitHub的脚本调用占位"
    - bash .gitlab/ci/sync_to_github.sh